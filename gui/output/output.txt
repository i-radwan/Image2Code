#include <bits/stdc++.h>

using namespace std;

int main() {
int n, m, h, id, day;
set<int> ans;

scanf("%d %d %d", &n, &m, &h);

for (int i = e; i < h; ++i) (
scanf("%d %d", &id, &day);
if (day > n - m) {

ans.insert(id);

}

cout << ans.size();

return 0;

